# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
  <input type="hidden" name="_method" value="PUT">が出力される

### findメソッドの引数に指定しているIDは何のIDか
  データベースのidカラムの値

### findメソッドで実行しているSQLは何か
  SELECT文で、さらにWHEREで範囲を絞っている

### findメソッドで取得できる値は何か
  主キーに一致するレコードを表すEloqientモデルのインスタンス

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか


## Todo論理削除

### traitとclassの違いとは
  traitは複数のクラス間でコードを再利用したい場合に用いる。インスタンス化はできない。

  classはオブジェクトの設計図のようなもので、同じ種類のオブジェクトを作成するためのテンプレートとして使う。
  さらに、継承することで、親クラスのプロパティやメソッドを子クラスで利用することができる。

### traitを使用するメリットとは
  ・コードの再利用が可能になる。
  ・PHPではクラスの継承は１つの親クラスに限るが、traitを利用することで複数の機能を利用することができる。

## その他

### フレームワークとは何か
  

### MVCはどういったアーキテクチャか

### ORMとは何か、またLaravelが使用しているORMは何か

### composer.json, composer.lockとは何か

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
